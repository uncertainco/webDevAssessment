function abc(first,last){
    return (first+" "+last); 
}
function bcd(first,last){
    first=prompt();
    last=prompt();
    var fullname=abc(first,last);
    alert("your name is "+fullname)
}
bcd()


// few statements in the task are written in a wrong way
